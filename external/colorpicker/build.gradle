apply plugin: 'com.android.library'

android {
    sourceSets.main {
        manifest.srcFile 'AndroidManifest.xml'
        java.srcDirs = ['src']
        res.srcDirs = ['res']
    }
    defaultConfig {
        minSdkVersion = 15
        targetSdkVersion 17
    }
}
